Drop your hero photo here, named exactly `hero-canopy.jpg`.

See the root `README.md` section "Where to put your photos" for full details and the
gallery photo naming convention (gallery photos go through Supabase Storage / the
`/admin` panel instead, not this folder).
